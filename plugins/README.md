# Custom Expo plugins

LOUSA Moon 1.18.4 currently does not require a project-local custom Expo config plugin.
This directory is intentionally present so future native changes are added explicitly here instead of being hidden in build scripts.
